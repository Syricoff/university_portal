Generic single-database configuration with Flask-Migrate.

Эта директория содержит файлы миграций базы данных, созданные с помощью Flask-Migrate.
Для управления миграциями используйте следующие команды:

# Инициализация миграций
flask db init

# Создание миграции
flask db migrate -m "Описание изменений"

# Применение миграций
flask db upgrade

# Откат миграций
flask db downgrade 